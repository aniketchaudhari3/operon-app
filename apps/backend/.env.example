# Comma-separated allowed browser origins (required for separate Vercel projects)
CORS_ORIGINS=http://localhost:3000

# Optional: frontend deployment URL (auto-added to CORS when set)
# FRONTEND_URL=https://your-frontend.vercel.app
